{
  "id": "init",
  "doc_root": "/var/www/html",
  // optional -- secure the entire server with http digest auth
  "digest_auth": {
    "realm": "private area",
    "user": "username",
    "password": "password"
  }
  // todo: add list of URL patterns that should be excluded from auth. needed to allow github callbacks into jenkins, for example
}