{
  "id": "config-metadata",
  "categories": {
      "init": {
      "fields": {
        "smtp_relay.username": { "required": true },
        "smtp_relay.password": { "required": true, "type": "secret_key" },
        "smtp_relay.host": { "required": true, "type": "hostname" },
        "smtp_relay.port": { "required": true, "type": "port" }
      }
    }
  }
}